

export default (state = _nullSession, action) => {

    Object.freeze(state);

    switch (action.type) {
        case value:
            
            break;
    
        default:
            return state;
    }
}